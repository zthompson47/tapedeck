"""The Icecast streaming server."""
# import reel


# class Icecast:
#     """A streaming server."""
#
#     def __init__(self, host, port, mount, password):
#         """Initialize an icecast subprocess."""
#         # self._exe = 'icecast'
#         # self._flags = ['-c', '{config_file}']

# async with Icecast() + Aria2() + Postgres() as server:
#     async with playlist | out as player:
#         async with trio.open_nursery() as nursery:
#             await server.start_daemon(nursery)
#             await player.start_daemon(nursery)
#             # loop & serve
#             await player.stop()
#             await server.stop()
